{
  "name": "fsharp",
  "version": "1.0.0",
  "description": "F Sharp command interpreter",
  "region": "sequenceDetail",
  "readable": "fsharp",
  "scripts" : {
    "build" : "npm install && webpack -d main.js"
  }
}
